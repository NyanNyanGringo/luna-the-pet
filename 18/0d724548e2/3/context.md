# Session Context

## User Prompts

### Prompt 1

python3.11 -m venv backend/.venv
backend/.venv/bin/pip install --upgrade pip
backend/.venv/bin/pip install -r backend/requirements.txt -r backend/requirements-dev.txt


Можем упростить данные команды?

### Prompt 2

А если предварительно зайти в backend папку и от туда запускать команды?

### Prompt 3

А если активировать виртуальное окружение, чтобы не прописывать пути к pip?

