Сделал ревью после реализации всех фаз этапа @specs/005-remove-oauth/ 

Findings

  1. HIGH — После удаления OAuth остались интеграционные тесты, которые всё ещё требуют удалённый callback route. См.
     test_regression_runtime_paths.py#L155 и test_regression_runtime_paths.py#L164.
     В diff удалены auth.py и регистрация роутера в main.py, но тесты всё ещё делают assert "/api/auth/openai/callback" in
     route_paths и assert response.status_code != 404.
     Я это воспроизвёл точечно через pytest: оба теста падают, route действительно отсутствует, а GET /api/auth/openai/callback
     возвращает 404. Это прямое нарушение Phase 5 / T018: “все оставшиеся тесты должны пройти”.
  2. MEDIUM — Downgrade-миграция восстанавливает oauth_credential неэквивалентно предыдущей схеме. См.
     drop_oauth_credential_table.py#L36 и drop_oauth_credential_table.py#L42.
     provider и status создаются как nullable=True, тогда как историческая схема была nullable=False, см.
     initial_phase2_models.py#L82 и initial_phase2_models.py#L86. Для US4 это означает, что rollback не восстанавливает “полную
     схему” точно.

  Вердикт
  REJECT

  Синтез

  - Finding 1 подтверждён всеми тремя ревьюерами и локальной проверкой.
  - Finding 2 принимаю, но не как blocker уровня #1.
  - Не принимаю как blocker замечание про README: там хвосты есть, но у вас отдельно запрещено трогать README без явного
    разрешения.
  - Не принимаю как отдельный blocker замечание про “нет покрытия новой revision”: это реальный пробел в верификации, но сам по
    себе он слабее уже найденного фактического падения тестов.

---

[Request interrupted by user for tool use]

---

Реализуй план

---

Нужно исправить blocker в задаче `specs/005-remove-oauth`, исходя из нового решения: проект ещё не был в проде, dev-среда
  пересоздаётся с нуля, сохранять обратимую историю OAuth не нужно. Вместо починки `downgrade()` надо переписать историю
  миграций так, чтобы `oauth_credential` вообще никогда не существовала в актуальной цепочке миграций.

  Контекст:
  - Сейчас blocker в `REDACTED.py`: `downgrade()` создаёт
  legacy-схему через `family_id`, хотя предыдущая ревизия уже workspace-centric.
  - Мы НЕ хотим сохранять rollback для OAuth.
  - Мы хотим убрать сам источник проблемы: из истории миграций должна исчезнуть необходимость отдельно удалять
  `oauth_credential`.

  Что нужно сделать:
  1. Изучи текущую цепочку миграций в `backend/alembic/versions/`.
  2. Перепиши миграционную историю так, чтобы:
     - таблица `oauth_credential` не создавалась в актуальной цепочке миграций;
     - не было отдельной миграции “drop oauth_credential table”, если она больше не нужна;
     - итоговая схема `head` соответствовала текущему runtime-коду без OAuth.
  3. Скорее всего потребуется:
     - удалить `REDACTED.py`;
     - убрать создание/трансформацию `oauth_credential` из более ранних миграций, где это уместно;
     - поправить тесты миграций, которые сейчас ожидают наличие `oauth_credential`.
  4. Проверь, нет ли больше в runtime-цепочке миграций и тестах зависимостей на `oauth_credential`, кроме допустимых
  исторических/spec-упоминаний.
  5. Не делай workaround вида “сломанный downgrade” или `NotImplementedError`. Нужно именно убрать OAuth из миграционной
  истории.
  6. Не трогай git stage/unstage, не делай commit/push.

  Технические требования:
  - Следуй `AGENTS.md`.
  - Весь нон-код текст на русском.
  - Перед правками сначала разберись, какие именно миграции и тесты завязаны на `oauth_credential`.
  - Если затронешь документацию, README не обновляй без отдельного разрешения пользователя.

  Что считаю успешным результатом:
  - В актуальной migration chain нет отдельного удаления `oauth_credential`, потому что таблица больше не появляется.
  - `head`-схема совместима с текущим кодом без OAuth.
  - Тесты миграций и остальные релевантные тесты обновлены под новую историю.
  - Команда `backend/.venv/bin/pre-commit run --all-files` успешно проходит.
  - После этого запущены все доступные тесты, и они зелёные. Если `backend/.venv` отсутствует, сначала аккуратно выясни, каким
  способом в этом проекте реально запускаются pre-commit и тесты, но не игнорируй этот шаг.

  Формат ответа:
  - Сначала коротко опиши, какие файлы меняешь и почему.
  - Потом внеси изменения.
  - В конце дай список findings/рисков, если что-то осталось спорным.
  - Обязательно отдельно сообщи, чем и как проверил результат.

---

Продолжай