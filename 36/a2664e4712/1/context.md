# Session Context

## User Prompts

### Prompt 1

## User Input

```text
Бот должен работать только при добавлении в группу. Если запускать бота без добавления в группу, он просит создать группу и дает инструкцию как это сделать краткую. Таким образом функциона расшаривания сессий не нужен, так как бот живет внутри группы и привязы�...

### Prompt 2

[Request interrupted by user]

### Prompt 3

## User Input

```text
Бот должен работать только при добавлении в группу. Если запускать бота без добавления в группу, он просит создать группу и дает инструкцию как это сделать краткую. Таким образом функциона расшаривания сессий не нужен, так как бот живет внутри группы и привязы�...

### Prompt 4

@.codex/skills/commit-message-skill/SKILL.md исполни скилл

### Prompt 5

Если бот уже привязан к другой группе (singleton-модель: один deploy = одна семья = одна группа), он отклоняет добавление в новую группу с сообщением «Я уже привязан к другой группе» и покидает новую группу (или остаётся неактивным).


Что это значит?

### Prompt 6

Бот должен работать в разных группах. Группы в данном случае - это аналоги workspace. У одного пользователя, если он захочет, может быть несколько workspace (=групп). И бот будет один на все группы. Также, если впоследствии коммерциализировать данный проект, то бот должен уметь обрабат...

